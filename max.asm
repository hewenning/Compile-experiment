INT21   MACRO X
        MOV DL,X
	MOV AH,02
	INT 21H
	ENDM
STACK	SENGMENT
STA	DB 512 DUP(?)
TOP	EQU LENGTH STA
STACK	ENDS
DATA	SENGMENT
BUF	DB 'MAX NUMBER:',0DH,0AH,'$'
DATA1	DB 35H,07H,07H,0ABH,56H,12H
	DB 98H,0CDH,76H,0FAH,0BFH
BLOCK	DB 10
DATA	ENDS
CODE	SEGMENT
	ASSUME CS:CODE,DS:DATA,SS:STACK
START:	MOV AX,STACK
	MOV SS,AX
	MOV SP,TOP
	MOV AX,DATA
	MOV DS,AX
	MOV DX,OFFSET BUF
	MOV AH,09
	INT 21H
	MOV SI,OFFSET BLOCK
	MOV CX,[SI]
	DEC CX
	MOV SI,OFFSET DATA1
	MOV AL,[SI]
MAX1:	INC SI
	CMP AL,[SI]
	JA NEXT
	MOV AL,[SI]
NEXT:	LOOP MAX1
	MOV BL,AL
	MOV CL,04
	SHR AL,CL
	CMP AL,09
	JA MAX4
	ADD AL,30H
MAX2:	INT21,AL
	MOV AL,BL
	AND AL,0FH
	CMP AL,09
	JA MAX5
	ADD AL,30H
MAX3:	INT21 AL
	MOV AH,4CH
	INT 21H
MAX4:	ADD AL,37H
	JMP MAX2
MAX5:	ADD AL,37H
	JMP MAX3
CODE	ENDS
	END START
